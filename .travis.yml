language: bash
branches:
  only:
    - master
sudo: false
addons:
  apt:
    sources:
      - debian-sid    # Grab shellcheck from the Debian repo (o_O)
    packages:
      - shellcheck
script:
  - 'bash -n git-idm'
  - 'shellcheck git-idm'
